{
  "name": "aws-functions",
  "scripts": {
    "build": "webpack",
    "zip": "cd lib && zip ../function.zip index.js",
    "deploy": "aws lambda update-function-code --function-name tutorbook-mail --zip-file fileb://function.zip --publish",
    "all": "npm run build && npm run zip && npm run deploy"
  },
  "engines": {
    "node": "12"
  },
  "main": "lib/index.js",
  "devDependencies": {
    "dotenv-webpack": "^1.8.0",
    "ts-loader": "^7.0.5",
    "typescript": "^3.9.6",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  },
  "dependencies": {
    "await-to-js": "^2.1.1",
    "aws-sdk": "^2.712.0",
    "firebase-admin": "^8.13.0",
    "uuid": "^8.2.0"
  },
  "private": true
}
